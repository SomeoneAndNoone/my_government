import 'package:flutter/material.dart';

class SavedMessagesView extends StatelessWidget {
  const SavedMessagesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Saved Messages View'),
    );
  }
}
