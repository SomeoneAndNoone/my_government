import 'package:flutter/material.dart';

class AccountPageView extends StatelessWidget {
  const AccountPageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Account Page View'),
      ),
    );
  }
}
