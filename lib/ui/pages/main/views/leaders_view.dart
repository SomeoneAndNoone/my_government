import 'package:flutter/material.dart';

class LeadersView extends StatelessWidget {
  const LeadersView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Leaders Page View'),
      ),
    );
  }
}
