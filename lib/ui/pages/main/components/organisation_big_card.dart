import 'package:flutter/material.dart';

class OrganisationBigCard extends StatelessWidget {
  const OrganisationBigCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
